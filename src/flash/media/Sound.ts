import { Sound as PixiSound } from '@pixi/sound';

class Sound extends PixiSound {
  /* constructor() {
    super();
  }
  override play(startTime: number, loops: number): Sound {
    super.play({ start: startTime }, loops);
    return this;
  } */
}

export { Sound }
